require 'spec_helper'

describe Rails::Galapagos::Ci do
  it 'has a version number' do
    expect(Rails::Galapagos::Ci::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
